spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/citronix
    username: postgres
    password: AMINOX1999
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  docker:
    compose:
      enabled: false
  liquibase:
    enabled: false


server:
  port: 8080
